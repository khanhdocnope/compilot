# Render Deployment Configuration
# Copy this file to .env and update values for your deployment

# Frontend URL for CORS (set to your frontend domain on Render)
# Leave as '*' for development, but use specific domain for production
FRONTEND_URL=*

# Port for Flask app (Render sets PORT automatically)
PORT=5000

# Database path
DATABASE=urls.db
